{
  "name": "gestao-contabil-backend",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "API do Gestor Neto Contabilidade (Express + Prisma + TS)",
  "main": "dist/server.js",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development TS_NODE_PROJECT=tsconfig.json tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "date-fns": "^3.6.0",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "pino": "^9.4.0",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@types/node-cron": "^3.0.11",
    "cross-env": "^7.0.3",
    "pino-pretty": "^11.2.2",
    "tsx": "^4.16.2",
    "typescript": "^5.9.3"
  }
}
